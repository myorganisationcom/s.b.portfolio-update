/**
 * DEPRECATED — use `@/server/db/index.js` instead.
 *
 * This file is kept temporarily to avoid breaking any existing imports.
 * It re-exports the new DB adapter factory.
 */

export { default } from '../server/db/index.js';
